# Copy this file to keystore.properties (at the repo root) and fill in your values.
# keystore.properties is git-ignored — never commit it.
storeFile=../focusguard-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=focusguard
keyPassword=YOUR_KEY_PASSWORD
